/* 

Quando usar o forEach()

- Quando precisar executar efeito colateral para cada item de um array.

o que é efeito colateral:

Um efeito colateral(side-effect) é qualquer mudança de estado de uma aplicação, que pode ser percebida do lado de fora de uma aplicação, que pode ser percebida do lado de fora da função, e que não é um valor retornado pela função

Alguns exemplos de side-effects:

- Modificar alguma variável ou propriedade de objeto que está do lado de fora da função
- Logar no console
- Manipular DOM
- Desencadear qualquer processo do lado de fora da função
- Invocar outra função que executa efeito colateral

*forEach não deve ser usado para retornar valor

Função que nao retorna valor,por baixo dos panos retorna undefined

for x forEach

forEach:

- precisa de um array inicial
- legibilidade
- menos código
- efeito colateral


for: 

- efeito colateral
- Break/ interrupção do loops  











*/ 