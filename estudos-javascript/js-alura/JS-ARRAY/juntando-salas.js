const salaJavaScript = ["Ana", "Maria", "Clara"];
const salaPython = ["Maju", "Ju", "Julia"];

// é preciso criar uma const para o novo array que vai surgir dessa concatenação

//sempre ficar atento sobre os metodos se eles retornam um novo array ou não

const salasUnificadas = salaJavaScript.concat(salaPython);

console.log(salasUnificadas);